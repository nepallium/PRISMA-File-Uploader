<div class="files">
    <% if (locals.files) { %>
        <% files.forEach(file => { %>
         <div class="file">
            <%= file.fileName %>
            <%= file.uploadedAt %>
        </div>
        <% }) %>
    <% } %>
</div>